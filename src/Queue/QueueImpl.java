/*
package Queue;

import java.util.Iterator;




import static sun.java2d.cmm.ColorTransform.In;

*/
/**
 * Created by jordan on 4/10/16.
 *//*

public class QueueImpl<Item> implements Queue, Iterable<Item> {

    // create an empty queue
    private QueueImpl(){

    }

    // add an item
    private void enqueue(Item item) {

    }

    // remove the most recently added item
    private Item dequeue() {

    }

    // is the queue empty?
    private boolean isEmpty() {

    }

    // number of items in the queue
    private int size() {

    }

    // Sample Queue Client
    private static int[] readAllInts(String name) {
        InImpl in = new InImpl(name);
        Queue<Integer> q = new QueueImpl<Integer>();
        while (!in.isEmpty())
            q.enqueue(in.readInt());
        int N = q.size();
        int[] a = new int[N];
        for (int i = 0 ; i < N ; i++)
            a[i] = q.dequeue();
        return a;
    }


    public static void main(String[] args) {
        Queue<Item> queue = new QueueImpl<Item>();

    }

}
*/
